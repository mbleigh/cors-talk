# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'compass' do
  watch(%r{^sass/(.*)\.s[ac]ss})
end

guard 'haml', output: '.', input: 'haml' do
  watch(/^.+(\.haml)/)
end
